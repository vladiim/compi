%section
  %header
    %h1 Get report
    %h4 We're still building our reporting tool. Let us know your email and the competitor site you're interested in and we'll send you a report when we've finished building.

  %section
    = form_tag '/reports', method: 'post' do
      = partial '/forms/errors', locals: { errors: errors } if defined?(errors)
      = field_set_tag(class: 'form-inputs') do
        %p
          = label_tag :email, required: true
          = email_field_tag :email, value: params[:email], placeholder: 'your@email.com'

        %p
          = label_tag :competitor_website, required: true
          = url_field_tag :competitor_website, value: params[:website], placeholder: 'http://www.competitor-site.com'

        .checkbox
          %label
            = check_box_tag :subscribe
            %p Get CI updates, no junk just a weekly round up of CI articles

      = field_set_tag(class: 'buttons') do
        = submit_tag 'Get report', class: 'btn btn-default'